<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             BackgroundColor="#4e4e4e"
             x:Class="Bro2Bro.Mobile.Views.LoginPage">
  <Grid VerticalOptions="Center">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <Image Grid.Row="0" Source="Icon" HorizontalOptions="Center" />
    <Label Grid.Row="1" Text="bro2bro" FontSize="14" TextColor="White" HorizontalOptions="Center" />

    <Grid Grid.Row="2" HorizontalOptions="FillAndExpand" Padding="20,10,20,10">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>

      <Entry Grid.Row="0" Placeholder="username" Text="{Binding Username}" />
      <Entry Grid.Row="1" IsPassword="True" Placeholder="password" Text="{Binding Password}" />
      <Switch Grid.Row="2" IsToggled="{Binding EnableRemember}" />
      <Button Grid.Row="3" Text="login" Clicked="btnLogin_Clicked" IsEnabled="{Binding EnableLoginButton}" x:Name="btnLogin" />
    </Grid>
   
    <ActivityIndicator Grid.Row="0" Grid.RowSpan="3" Color="Red" IsRunning="{Binding EnableRunningIndicator}" />
  </Grid>
</ContentPage>